import LocalizedPage from"../../i18n/LocalizedPage";import{dictionaries,isLocale,locales,type Locale}from"../../i18n/config";
export async function generateMetadata({params}:{params:Promise<{locale:string;slug:string[]}>}){const{locale:raw,slug}=await params;const locale=(isLocale(raw)?raw:"en")as Locale;const d=dictionaries[locale];const path=slug.join("/");const languages=Object.fromEntries(locales.map(code=>[code,`/${code}/${path}`]));return{title:d.meta.title,description:d.meta.description,alternates:{canonical:`/${locale}/${path}`,languages:{...languages,"x-default":`/en/${path}`}},openGraph:{title:d.meta.title,description:d.meta.description,locale:d.meta.locale,url:`/${locale}/${path}`}}}
export default async function Page({params}:{params:Promise<{locale:string;slug:string[]}>}){const{locale,slug}=await params;return <LocalizedPage locale={isLocale(locale)?locale:"en"} slug={slug}/>}
